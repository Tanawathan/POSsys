# Redirect all traffic to index.html for SPA routing
/*    /index.html   200

# Optional: Force HTTPS  
http://tanawat-order-system.netlify.app/*    https://tanawat-order-system.netlify.app/:splat    301!

# Cache static assets
/assets/*    /assets/:splat    200    Cache-Control: max-age=31536000

# Note: API proxy rules require environment variables to be configured in Netlify
# These will be dynamically generated during build process
