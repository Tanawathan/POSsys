# Redirect all traffic to index.html for SPA routing
/*    /index.html   200

# Cache static assets
/assets/*    /assets/:splat    200    Cache-Control: max-age=31536000

# Note: API proxy rules have been moved to JavaScript environment variables
# to avoid exposing sensitive webhook URLs in build output
